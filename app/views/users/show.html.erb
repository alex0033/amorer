<div class="main-contents col-md-8">
  <h3><%= @user.name %></h3>
  <div class="self_introduction">
    <% if text = @user.self_introduction.presence %>
      <p><%= safe_join(text.split("\n"), tag(:br)) %><p>
    <% else %>
      <p>自己紹介が未入力です。</p>
    <% end %>
  </div>
  <% if @user == current_user %>
    <%= link_to "プロフィール編集はこちら", edit_user_registration_path %>
  <% end %>
</div>
